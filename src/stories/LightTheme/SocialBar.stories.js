import React from 'react';
import { SocialBar } from '../../components/SocialBar/SocialBar';

export default {
  title: 'Light Theme/Social Bar',
  component: SocialBar,
};

const Template = (args) => <SocialBar {...args} />;

export const Primary = Template.bind({});


